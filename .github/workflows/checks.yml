name: checks
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch godot-cassie binary
        run: |
          # TODO: pin to a release asset from V-Sekai-fire/entities-godot at
          # feat/module-cassie once the CI job publishes one.
          echo "godot binary fetch: not yet wired"
          exit 78
      - name: Run checks
        run: |
          chmod +x checks/run.sh
          checks/run.sh ./godot
