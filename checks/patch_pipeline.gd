extends SceneTree

# Patch pipeline: fixture curvenet -> recorded face count and vertex hash.
# Control: scrambling one knot must change the vertex hash.

func _init() -> void:
	print("PENDING patch_pipeline: replace with the fixture once the surface stabilises")
	quit(0)
