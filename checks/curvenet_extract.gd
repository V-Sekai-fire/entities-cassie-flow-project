extends SceneTree

# Curvenet extraction: a planted 3-cycle sketch produces three knots.
# Control: a sketch with one edge removed must NOT extract a 3-cycle.

func _init() -> void:
	print("PENDING curvenet_extract: replace with the fixture once the surface stabilises")
	quit(0)
