extends SceneTree

# Constraint solver: over- and under-constrained fixtures return documented codes.
# Control: an over-constrained system that "solves" is a FAIL.

func _init() -> void:
	print("PENDING constraint_solver: replace with the fixture once the surface stabilises")
	quit(0)
